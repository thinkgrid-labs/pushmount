{
  "name": "@pushmount/fastify",
  "version": "0.1.0-rc.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test"
  },
  "peerDependencies": {
    "fastify": ">=4"
  },
  "devDependencies": {
    "@pushmount/server": "workspace:*",
    "@types/node": "^22.20.1",
    "fastify": "^5.11.3",
    "typescript": "^5.9.3"
  },
  "license": "MIT OR Apache-2.0",
  "engines": {
    "node": ">=22"
  }
}

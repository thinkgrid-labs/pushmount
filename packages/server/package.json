{
  "name": "@pushmount/server",
  "version": "0.1.0-rc.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test && node ../../conformance/runner.mjs ./dist/conformance-adapter.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "compression": "^1.8.1",
    "express": "^5.2.1",
    "typescript": "^5.6.0"
  },
  "license": "MIT OR Apache-2.0",
  "engines": {
    "node": ">=22"
  }
}
